NEXT_PUBLIC_APP_URL=https://bell24h.com
DATABASE_URL=postgresql://user:pass@ep-cool-name-123456.ap-south-1.aws.neon.tech/db?sslmode=require
N8N_WEBHOOK_URL=https://n8n.bell24h.com/webhook
AI_API_URL=https://api.bell24h.com/v1

# Bell24h Production Environment Configuration
# For 1000+ concurrent users with enterprise-grade security

# Database Configuration (PostgreSQL with connection pooling)
DATABASE_URL="postgresql://bell24h_user:${DB_PASSWORD}@${DB_HOST}:5432/bell24h_prod?schema=public&connection_limit=20&pool_timeout=20&connect_timeout=60"

# NextAuth Configuration
NEXTAUTH_URL="https://www.bell24h.com"
NEXTAUTH_SECRET="${NEXTAUTH_SECRET}"

# OAuth Providers
GOOGLE_CLIENT_ID="${GOOGLE_CLIENT_ID}"
GOOGLE_CLIENT_SECRET="${GOOGLE_CLIENT_SECRET}"

LINKEDIN_CLIENT_ID="${LINKEDIN_CLIENT_ID}"
LINKEDIN_CLIENT_SECRET="${LINKEDIN_CLIENT_SECRET}"

GITHUB_CLIENT_ID="${GITHUB_CLIENT_ID}"
GITHUB_CLIENT_SECRET="${GITHUB_CLIENT_SECRET}"

# Payment Gateways
RAZORPAY_KEY_ID="${RAZORPAY_KEY_ID}"
RAZORPAY_KEY_SECRET="${RAZORPAY_KEY_SECRET}"

STRIPE_PUBLISHABLE_KEY="${STRIPE_PUBLISHABLE_KEY}"
STRIPE_SECRET_KEY="${STRIPE_SECRET_KEY}"
STRIPE_WEBHOOK_SECRET="${STRIPE_WEBHOOK_SECRET}"

# AI Services
OPENAI_API_KEY="${OPENAI_API_KEY}"
AZURE_SPEECH_KEY="${AZURE_SPEECH_KEY}"
AZURE_SPEECH_REGION="${AZURE_SPEECH_REGION}"

# Email Service
SMTP_HOST="${SMTP_HOST}"
SMTP_PORT="${SMTP_PORT}"
SMTP_USER="${SMTP_USER}"
SMTP_PASSWORD="${SMTP_PASSWORD}"
SMTP_FROM="${SMTP_FROM}"

# SMS Service
TWILIO_ACCOUNT_SID="${TWILIO_ACCOUNT_SID}"
TWILIO_AUTH_TOKEN="${TWILIO_AUTH_TOKEN}"
TWILIO_PHONE_NUMBER="${TWILIO_PHONE_NUMBER}"

# File Storage
AWS_ACCESS_KEY_ID="${AWS_ACCESS_KEY_ID}"
AWS_SECRET_ACCESS_KEY="${AWS_SECRET_ACCESS_KEY}"
AWS_REGION="${AWS_REGION}"
AWS_S3_BUCKET="${AWS_S3_BUCKET}"

# Redis Cache (for session storage and caching)
REDIS_URL="${REDIS_URL}"

# Monitoring and Analytics
SENTRY_DSN="${SENTRY_DSN}"
GOOGLE_ANALYTICS_ID="${GOOGLE_ANALYTICS_ID}"

# Security
JWT_SECRET="${JWT_SECRET}"
ENCRYPTION_KEY="${ENCRYPTION_KEY}"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Performance
NODE_ENV="production"
NEXT_TELEMETRY_DISABLED=1

# CDN Configuration
CDN_URL="${CDN_URL}"

# Blockchain (if implemented)
ETHEREUM_RPC_URL="${ETHEREUM_RPC_URL}"
POLYGON_RPC_URL="${POLYGON_RPC_URL}"
WALLET_PRIVATE_KEY="${WALLET_PRIVATE_KEY}"

# Feature Flags
ENABLE_VOICE_RFQ=true
ENABLE_VIDEO_RFQ=true
ENABLE_AI_MATCHING=true
ENABLE_BLOCKCHAIN=false
ENABLE_MOBILE_APP=true

# Logging
LOG_LEVEL="info"
LOG_FORMAT="json"